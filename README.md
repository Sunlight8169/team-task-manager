# team-task-manager
A full-stack Team Task Management Web Application that enables users to create projects, assign tasks, and track progress with role-based access (Admin &amp; Member). Built using React (Vite) for frontend, Flask for backend, and MySQL for database, with secure authentication and RESTful APIs. Deployed on Railway for public access.
